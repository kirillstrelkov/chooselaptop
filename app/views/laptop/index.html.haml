%h1 Laptop#index
%p Find me in app/views/laptop/index.html.haml

=form_tag '/laptop/sort', :method => :post do
  =text_area_tag 'laptops', @query, :autofocus => true ,:placeholder => "Enter many many laptops use 2 enters as a delimiter"
  %div{:id => 'form-buttons'}
    =button_tag 'Clear', :id=> 'clear_btn', :type => 'submit', :name => 'commit', :value => 'clear'
    =button_tag 'Sort laptops', :id=> 'search_btn', :type => 'submit', :name => 'commit', :value => 'sort'
-if !@sorted_laptops.empty?
  %p Sorted list of phone best-to-worst:
%table
  %tr
    %th Average index
    %th CPU index
    %th CPU model
    %th GPU index
    %th GPU model
  -@sorted_laptops.each  do |laptop|
    %tr
      %td
        =laptop[:avg_index]
      %td
        =laptop[:cpu_index]
      %td
        =laptop[:cpu]
      %td
        =laptop[:gpu_index]
      %td
        =laptop[:gpu]
    %tr
      %td{:colspan => 5}
        =laptop[:desc]
